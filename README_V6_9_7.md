# Nicole Puzzle Coach V6.9.7 – Training Data Mapping Fix

- Mobile Training card now reads MSP median and latest solve from `median_target`.
- `vs. Median` is calculated from the exact MSP median and latest solve when no precomputed delta exists.
- WM-Fit now extracts the numeric `score` from the WM-Fit object instead of rendering `[object Object]`.
- No layout, navigation, readiness calculation, recommendation logic, or MySpeedPuzzling synchronization changes.
