TRAINING UI POLISH + WEEKLY SKIP

Changes:
- Skip button is now always visible in weekly-plan puzzle cards.
- Skip remains "borrowed / unavailable" only; nothing is deleted.
- Skipped puzzles still return through "Wieder verfügbar" / "Alle wieder verfügbar".
- "Heute trainieren" explanation and Skip action are visually separated.
- Training puzzle cards use one consistent mobile layout:
  same image size, spacing, rank alignment, text structure and margins.
- Median-gap, repeat-priority and unsolved cards use the same visible Skip pattern.

Deploy the complete project as usual.
