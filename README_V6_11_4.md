# V6.11.4
- Tournament rendering is no longer queued behind Swiss ranking.
- Swiss ranking has its own timeout and cannot block the Today page.
- Tournament UI resolves independently within 6.5 seconds.
- Resilient Mode is hidden whenever a valid MSP-derived persisted dataset exists.
- Legacy label removed from the visible MSP KPI when the data is valid MSP-derived data.
